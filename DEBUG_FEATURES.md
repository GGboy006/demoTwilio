# 调试功能说明

## 调试面板功能

我已经为 Twilio 视频协助系统添加了一个完整的调试日志面板，方便在 Demo 环境下查看详细的运行状态和调试信息。

### 主要特性

#### 1. 实时日志记录
调试面板会记录所有重要的操作和事件，包括：
- ✅ **成功日志**：连接成功、轨道附加成功等
- ℹ️ **信息日志**：网络质量变化、参与者状态等
- ⚠️ **警告日志**：参与者离开、连接断开等
- ❌ **错误日志**：连接失败、轨道附加失败等

#### 2. 日志过滤
- 可以按类型过滤：全部、成功、信息、警告、错误
- 每个过滤器会显示该类型的日志数量
- 方便快速定位特定类型的问题

#### 3. 详细信息展示
每条日志包含：
- 时间戳（精确到秒）
- 日志类型和图标
- 详细的消息描述
- 可选的详细信息（JSON 格式）

#### 4. 实用功能
- **折叠/展开**：点击面板头部可以折叠或展开
- **清除日志**：一键清空所有日志
- **导出日志**：导出为 .txt 文件，方便分享和分析
- **自动滚动**：新日志出现时自动滚动到顶部
- **日志限制**：最多保留 100 条日志，避免内存占用过多

### 记录的关键事件

#### 发起人页面 (InitiatorPage)
1. 页面初始化和参数
2. Twilio Token 获取过程
3. 视频房间连接状态
4. 本地视频/音频轨道附加
5. 音频监听启动
6. 网络质量变化
7. 远程参与者（接受人）加入/离开
8. 远程轨道订阅/取消订阅
9. 轨道附加/移除操作
10. 挂断和清理过程

#### 接受人页面 (HelperPage)
1. 页面初始化和参数
2. Twilio Token 获取过程
3. 视频房间连接（仅音频模式）
4. 本地音频轨道和监听
5. 网络质量变化
6. 远程参与者（发起人）加入/离开
7. 发起人视频/音频轨道接收
8. 拍照操作
9. 手电筒开关操作
10. 挂断和清理过程

### 使用方法

1. **打开应用**：调试面板会自动出现在页面底部
2. **查看日志**：面板默认展开，可以看到实时日志流
3. **过滤日志**：点击顶部的过滤按钮（全部、成功、信息、警告、错误）
4. **查看详情**：展开日志项可以看到详细的 JSON 数据
5. **折叠面板**：点击面板头部可以折叠，不影响视频通话
6. **导出日志**：点击导出按钮保存日志文件，方便后续分析

### 调试面板位置
- 固定在页面底部
- z-index: 9998，确保在视频上方
- 最大高度：屏幕高度的 50%
- 折叠时只显示标题栏

### 错误追踪
所有错误都会被捕获并记录，包括：
- 错误消息
- 错误代码（如果有）
- 堆栈跟踪（Stack trace）
- 相关上下文信息

### Demo 特性
这个调试面板专为 Demo 版本设计：
- 不影响正常的视频通话功能
- 可以随时折叠/展开
- 日志不会占用过多内存（最多 100 条）
- 清晰的视觉反馈和颜色编码
- 导出功能方便分享和报告问题

### 注意事项
- 调试面板仅用于开发和演示环境
- 生产环境建议移除或禁用（通过环境变量控制）
- 导出的日志文件可能包含敏感信息，请谨慎分享

## 设备测试功能

### 功能概述
新增了完整的设备测试模态框，用户可以在进入视频通话前测试和选择自己的设备。

### 主要功能

#### 1. 📹 视频设备测试
- **设备列表**：自动检测并列出所有可用的摄像头
- **实时预览**：选择摄像头后立即显示实时画面
- **多设备支持**：可以在多个摄像头之间切换
- **预览窗口**：300px 高度的视频预览窗口

#### 2. 🎤 音频输入测试
- **麦克风列表**：自动检测并列出所有可用的麦克风
- **音量检测**：实时显示麦克风输入音量
- **可视化音量条**：渐变色音量条（0-100%）
- **状态提示**：显示是否正在监听麦克风

#### 3. 🔊 扬声器测试（如果浏览器支持）
- **扬声器列表**：列出所有音频输出设备
- **测试音频**：播放 440Hz (A4音) 测试音，持续 1 秒
- **设备切换**：支持切换不同的扬声器设备
- **兼容性**：仅在支持 setSinkId API 的浏览器中可用

#### 4. 权限管理
- **一键授权**：提供"请求设备权限"按钮
- **权限状态**：实时显示摄像头、麦克风和权限状态
- **自动检测**：检测设备是否已授权（通过 label 判断）
- **刷新设备**：可以刷新设备列表

#### 5. 设备状态面板
显示三个状态指示器：
- ✅ 摄像头激活状态
- ✅ 麦克风激活状态
- ✅ 权限授予状态

#### 6. 错误处理
- **友好提示**：所有错误都会以友好的方式展示
- **错误类型**：显示具体的错误消息
- **自动清除**：切换设备时自动清除之前的错误

### 技术实现

#### 设备枚举
```javascript
navigator.mediaDevices.enumerateDevices()
```
- 获取所有视频输入、音频输入和音频输出设备

#### 视频预览
```javascript
navigator.mediaDevices.getUserMedia({ video: { deviceId: { exact: deviceId } } })
```
- 使用特定设备 ID 获取视频流
- 绑定到 video 元素进行预览

#### 音频监听
使用 Web Audio API 实时分析音频音量：
```javascript
AudioContext -> MediaStreamSource -> AnalyserNode
```
- 实时获取音频频率数据
- 计算平均音量并转换为百分比

#### 扬声器测试
```javascript
OscillatorNode -> GainNode -> AudioContext.destination
```
- 生成 440Hz 正弦波
- 音量设置为 30%，播放 1 秒

### 使用方法

1. **打开设备测试**：
   - 点击页面左上角的"🎥 设备测试"按钮
   - 模态框会弹出显示设备测试界面

2. **请求权限**：
   - 首次使用时，点击"🔐 请求设备权限"
   - 浏览器会请求摄像头和麦克风权限
   - 授权后设备列表会显示完整的设备名称

3. **测试摄像头**：
   - 从下拉菜单选择摄像头
   - 实时预览窗口会显示摄像头画面
   - 可以切换不同的摄像头对比效果

4. **测试麦克风**：
   - 从下拉菜单选择麦克风
   - 对着麦克风说话
   - 观察音量条是否有变化

5. **测试扬声器**（可选）：
   - 选择扬声器设备
   - 点击"🔊 测试扬声器"按钮
   - 应该能听到 1 秒的测试音

6. **完成测试**：
   - 确认设备工作正常后
   - 点击"完成测试"关闭模态框
   - 返回主页面进入视频房间

### 浏览器兼容性

| 功能 | Chrome | Firefox | Safari | Edge |
|------|--------|---------|--------|------|
| 视频预览 | ✅ | ✅ | ✅ | ✅ |
| 音频监听 | ✅ | ✅ | ✅ | ✅ |
| 扬声器切换 | ✅ | ❌ | ❌ | ✅ |
| 设备枚举 | ✅ | ✅ | ✅ | ✅ |

**注意**：
- 扬声器切换需要浏览器支持 `HTMLMediaElement.setSinkId()` API
- Firefox 和 Safari 不支持扬声器切换
- 所有现代浏览器都支持基本的设备测试功能

### 自动清理
组件会在以下情况自动清理资源：
- 关闭模态框时
- 切换设备时
- 组件卸载时

清理内容包括：
- 停止所有媒体流
- 取消动画帧
- 关闭 AudioContext
- 停止测试音频

### 按钮位置
- **设备测试按钮**：左上角，蓝色背景
- **重置调试按钮**：右上角，橙色背景
- 两个按钮 z-index 都是 9999，确保始终可见

### 响应式设计
- 模态框最大宽度：700px
- 最大高度：90vh（可滚动）
- 移动端友好的触摸体验
- 自适应不同屏幕尺寸

### 最佳实践建议

1. **首次使用**：建议用户首次使用前先进行设备测试
2. **设备切换**：如果更换了摄像头或麦克风，重新测试确认
3. **问题排查**：视频通话有问题时，先用设备测试检查硬件
4. **权限问题**：如果设备列表为空或没有 label，需要请求权限
5. **多设备用户**：有多个摄像头/麦克风的用户可以选择最佳设备

### 安全与隐私
- 设备测试完全在本地进行
- 不会上传任何音视频数据
- 关闭模态框后立即释放所有设备
- 符合浏览器安全策略和用户隐私要求
